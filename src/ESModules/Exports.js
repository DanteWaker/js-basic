import { soma, pi } from "./src/ESModules/Soma";

console.log(soma(1, 2))

/**
 * Exportar com export obriga vc a usar o mesmo nome que exportou
 * Chamado de named export
 * 
 * O Export default nao te obriga a usar o mesmo nome, ja que ele exporta um unico elemento
 * por arquivo
 * 
 */